{
  "name": "@sqlitewasmblazor/worker-common",
  "version": "1.0.0",
  "description": "Shared TypeScript worker infrastructure for SqliteWasmBlazor. Consumed by plane-1's worker (TypeScript/) and plane-2's worker (Phase 4). Provides worker state singletons, logger, MessagePack <-> SQLite value conversion, plain bulk-insert path, EF Core SQL helpers, and the request/response envelope types.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "dependencies": {
    "@sqlite.org/sqlite-wasm": "3.53.0-build1",
    "msgpackr": "2.0.5"
  }
}
