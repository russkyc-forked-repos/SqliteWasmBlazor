@inherits LayoutComponentBase

<MudThemeProvider/>
<MudPopoverProvider/>
<MudDialogProvider/>
<MudSnackbarProvider/>

<ServiceWorkerUpdateNotification/>

<WindowProvider>
    <MudLayout>
        <MudAppBar Elevation="1">
            <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start"
                           OnClick="@ToggleDrawer"/>
            <MudText Typo="Typo.h6">SqliteWasm Blazor Demo</MudText>
            <MudSpacer/>
            @* Status sink for every Crypto.UI command formatter; renders snackbar
               notifications + (depending on mode) inline icons. The icons render
               in place, so this has to sit inside the bar to have a place. *@
            <StatusDisplay/>
            <MudIconButton Icon="@Icons.Material.Filled.Info" Color="Color.Inherit"
                           Href="https://github.com/b-straub/SqliteWasmBlazor" Target="_blank"/>
        </MudAppBar>
        <MudDrawer @bind-Open="_drawerOpen" ClipMode="DrawerClipMode.Always" Elevation="2">
            <NavMenu/>
        </MudDrawer>
        <MudMainContent>
            <MudContainer MaxWidth="MaxWidth.Large" Class="my-4">
                @Body
            </MudContainer>
        </MudMainContent>
    </MudLayout>
</WindowProvider>

@code {
    private bool _drawerOpen = true;

    private void ToggleDrawer()
    {
        _drawerOpen = !_drawerOpen;
    }

}
